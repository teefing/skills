# Field Metadata: UI Controls

This reference covers read-only UI components in the table.

## Workflow

### Stage (`type=24`, `ui_type=Stage`)

- Read-only workflow stage indicator.

## Interactive

### Button (`type=3001`, `ui_type=Button`)

- Interactive button to trigger automation. Read-only via API.
